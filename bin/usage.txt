Usage: pplink [options] <source> [<destination>]

Options:

--help, -h
    Show this message

--relative, -r
    Create a link using the relative path instead of the absolute path

--name
    Override the name used for the directory and in the package.json

--save, --save-dev, --save-optional, --save-bundle
    Save the dependency in the destination's package.json
